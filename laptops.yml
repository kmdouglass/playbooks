---
- name: Runs local provisioning steps for a laptop
  hosts: local
  roles:
    - common
    - git
    - dotfiles
  vars:
    - home_dirs:
        opt: opt
        src: src
        venvs: venvs
    - username: kmdouglass
